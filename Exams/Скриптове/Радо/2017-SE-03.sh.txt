#!/bin/bash

if [ $(whoami) = "root" ]; then
        while read -r line; do
                find "${line}" -mindepth 1 -type f -exec stat '%X %n' {} \; | sort -n | head -n 1 > lines.txt
        done < <(cat /etc/passwd | cut -d ':' - f 6)
fi

cat lines.txt | sort -n | head -n 1
